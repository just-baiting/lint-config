{
  "name": "eslint-config",
  "author": "Michael Nguyen",
  "license": "ISC",
  "description": "",
  "private": true,
  "scripts": {
    "lerna:build": "lerna run build",
    "lerna:clean": "lerna clean --yes",
    "lerna:release": "lerna publish from-package --yes",
    "lerna:release-canary": "lerna publish --canary",
    "lerna:version": "lerna version",
    "turbo:build": "turbo run build",
    "version": "changeset version"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/just-baiting/eslint-config.git"
  },
  "bugs": {
    "url": "https://github.com/just-baiting/eslint-config/issues"
  },
  "homepage": "https://github.com/just-baiting/eslint-config#readme",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@just-baiting/prettier-config": "^0.0.1",
    "husky": "^6.0.0",
    "lerna": "^4.0.0",
    "lint-staged": "^10.5.4",
    "turbo": "^1.2.8"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.js": "eslint --fix"
  },
  "dependencies": {
    "@changesets/cli": "^2.22.0"
  },
  "version": "0.0.2"
}
